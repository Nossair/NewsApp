<!DOCTYPE html>

<title>GroupMail</title>

{% block body %}
    <h1>GroupMail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group_mail.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ group_mail.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_group_mail_index') }}">back to list</a>

    <a href="{{ path('app_group_mail_edit', {'id': group_mail.id}) }}">edit</a>

    {{ include('group_mail/_delete_form.html.twig') }}
{% endblock %}
